const noIndex = require('./noIndex/noIndex')
const notJson = require('./notJson/notJson')
const notObject = require('./notObject/notObject')
const ok = require('./ok/ok')

module.exports = {
  'no index': noIndex,
  'not json': notJson,
  'not object': notObject,
  'ok': ok
}