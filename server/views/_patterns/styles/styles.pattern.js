const Pattern = require('../../../../_patterns/pattern/_classes/Pattern/Pattern.class')

module.exports = new Pattern(
  'styles',
  'A folder which contains `.scss` files to be bundled in page(s) stylesheet.'
)